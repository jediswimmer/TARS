{
  "name": "@tars/provider-claude",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Anthropic Claude implementation of the TARS agent fleet. The reference provider — build here first, then mirror to grok/sol.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@tars/contracts": "workspace:*",
    "@tars/core": "workspace:*",
    "@tars/connectors": "workspace:*",
    "@anthropic-ai/sdk": "^0.111.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  }
}
