{
  "name": "@tars/connectors",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared data-source connectors (Azure first). Normalizes cloud state into a snapshot the scanner reasons over. Identical input for every provider.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@tars/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  },
  "//live-deps": "When implementing LiveAzureConnector, add: @azure/identity, @azure/arm-resourcegraph, @microsoft/microsoft-graph-client, @azure/arm-security"
}
