{
  "name": "@tars/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-provider bake-off harness. Scores identical pipeline runs from Claude, Grok, and Sol against a shared rubric.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@tars/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  }
}
