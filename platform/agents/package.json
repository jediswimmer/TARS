{
  "name": "@tars/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The shared agent fleet + prompts, parameterized by provider. All providers reuse these; only the injected LlmProvider (and optional prompt overrides) differ.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@tars/contracts": "workspace:*",
    "@tars/core": "workspace:*",
    "@tars/connectors": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  }
}
