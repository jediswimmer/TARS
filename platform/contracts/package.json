{
  "name": "@tars/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider-agnostic data contracts: artifact schemas + agent/port interfaces. The spine everything else depends on.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
